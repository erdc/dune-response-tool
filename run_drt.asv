%load structure
load example_input_and_output.mat

%run model
scenario = drt_model_instantaneous(scenario);

%plot model results
figure, 
plot(scenario.grids.Xgrid, scenario.outputs.Z)
xlabel('Cross Shore Disance (m)')
ylabel('Elevation (m)')

